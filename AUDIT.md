# Аудит сайта: технический, SEO, безопасность

- **Дата:** 23.08.2026
- **Объект:** `/Users/silrowd/new_site` — 37 HTML-страниц, PHP-роутер (`router.php`), админ-панель (`admin/admin.php`), новости в JSON (`data/news.json`), статические ассеты (`assets/`, `img/`).
- **Методика:** 5 автоматизированных Python-скриптов (битые ссылки, SEO-меты, версионирование ассетов, целостность изображений, формы) + ручная верификация ложных срабатываний (URL-кодирование, относительные пути от CSS) + проверка git (`git ls-files`).

---

## 🔴 Критичные

### 1. Hero-слайдер главной: битые пути к слайдам в CSS
- **Файл:** `assets/css/style.css`, строки 278–291.
- **Проблема:** 8 `url()` — `url("../../img/slide1.jpg")` … `url("../../img/slide4.webp")`. CSS лежит в `assets/css/`, поэтому `../../img/` выходит **за пределы корня сайта** (в `/Users/silrowd/img/` — папки не существует, проверено). Фон всех 4 слайдов на главной не отображается.
- **Правильно:** `../img/`.
- **Файлы на месте:** `img/slide1–4.jpg`, `img/slide1–4.webp` (битых изображений в HTML нет — только в CSS).
- **Статус:** ✅ исправлено 23.08.2026.

### 2. Плейсхолдер-телефон на objects.html
- **Файл:** `objects.html`, строки 37 и 205: `+7 (812) 000-00-00`, `tel:+78120000000`.
- На остальных 36 страницах — `+7 (812) 388-63-71`.
- **Статус:** ✅ исправлено 23.08.2026.

---

## 🟠 Важные (SEO / производительность / юридическое)

### 3. Рассинхрон cache-busting (`?v=`)
17 из 37 страниц ссылались на старые версии ассетов:
- CSS встречались варианты: `20260822`, `20260823`, `20260823ab`, `20260822h`, `20260822i`, `20260823a`.
- JS: у 6 vacancy-страниц — `20260822h`, у остальных — свежая версия.
После правок CSS часть посетителей видела старый кеш.
- **Статус:** ✅ унифицировано 23.08.2026: CSS → `?v=20260823b` (бамп, т.к. содержимое CSS изменилось), JS → `?v=20260823` на всех 37 страницах.

### 4. Нет `robots.txt` и `sitemap.xml`
При 37 индекс-страницах это заметно ограничивает охват.
- **Статус:** ✅ создано 23.08.2026 (`robots.txt`: allow всё, disallow `/admin/`, ссылка на sitemap; `sitemap.xml`: 37 URL на `https://sk-psp.ru`, lastmod 2026-08-23, приоритеты по типам страниц; XML валиден, перекрёстная сверка с файлами 1:1). Домен подтверждён заказчиком (подключение позже).

### 5. Нет Open Graph, canonical и JSON-LD
На ни одной странице. Для строительной компании с адресом в СПб критична схема `LocalBusiness` (телефон, адрес, гео).
- **Статус:** ⏳ в плане.

### 6. Четыре `<h1>` на главной
По одному на каждый hero-слайд. Для SEO предпочтителен один H1; слайды лучше на `<h2>`.
- **Статус:** ⏳ в плане.

### 7. Дубликаты title + H1 у двух новостей
`news-oyunost-2017-02-02.html` и `news-oyunost-2017-02-10.html` — одинаковый title «Новости ЖК „Светлый мир "О'Юность"»» и одинаковый H1 (контент разный, md5 текста различается). Поисковик может отфильтровать одну из двух. В title стоит добавить дату.
- **Статус:** ⏳ в плане.

### 8. Изображения: нет width/height, частично lazy
- 0 из 150 `<img>` имеют `width`/`height` → CLS (скачки макета при загрузке).
- `loading="lazy"` есть у 57/150 и только на 9 страницах (certificates, clients, contacts, index, 4 новости, objects) — неконсистентно.
- **Статус:** ⏳ в плане.

### 9. Юридическая информация
- На `contacts.html` и `about.html` нет ИНН/ОГРН (только адрес и телефон) — для юрлица в РФ публикация реквизитов фактически обязательна.
- В `privacy.html` **нет упоминания FormSubmit.co**, хотя форма отправляет имя/телефон на сторонний сервис (`formsubmit.co/ajax/bik-m@mail.ru`) — требует раскрытия в политике обработки ПДн (152-ФЗ).
- **Статус:** ⏳ в плане (нужны реквизиты от заказчика).

### 10. Длинные title/description
- `industrial.html` — title 115 символов, description 233 символа (обрежется в выдаче).
- 5 vacancy-страниц — title 71–82 символа; `public-buildings.html`, `shopping-malls.html` — 79.
- **Статус:** ⏳ в плане.

---

## 🟡 Мелкие

| # | Проблема | Детали |
|---|----------|--------|
| 11 | Незакоммиченное состояние | ~100 изменённых файлов + 15 untracked (новые слайды, webp-версии объектов) + 11 удалённых (старые `slide1.jpeg`, `slide2.png`, `BC.png` и др.). HTML уже ссылается на новые файлы. |
| 12 | Мёртвые файлы | `img/favicon-48x48.png` (подключены 16/32/ico/apple), `img/logo.gif` (legacy), пустая папка `img_news/` (загрузки админки идут в `img/news/`). |
| 13 | Внешние ссылки на клиентов через `http://` | 12 ссылок без https (index.html, clients.html). |
| 14 | Зависимость от Google Fonts | Roboto с preconnect; system-font fallback (Segoe UI) есть — допустимо, мониторинг LCP. |

**Статус №11:** ✅ закоммичено 23.08.2026.

---

## ✅ Что в порядке

- **Битых внутренних ссылок: 0.** 8 PDF с кириллицей в `labor-safety.html` — ложная тревога: URL-закодированные `%20`, все файлы на месте.
- **Отсутствующих изображений: 0.** Все 150 `<img>` + `img/news/old-*.jpg` из news.json существуют; у всех задан `alt`.
- **SEO-базис на 100% страниц:** title, meta description, viewport, `lang="ru"`, charset. Все title уникальны (кроме пары новостей, см. №7).
- **Навигация единая** (1 вариант на всех 37), футер-телефоны консистентны (после фикса №2).
- **Безопасность:**
  - `admin/` — 0 файлов в git (`.gitignore` работает), в `.DS_Store` тоже 0.
  - Админка: CSRF-токены, `hash_equals`, `session_regenerate_id`, проверка `getimagesize`, whitelist расширений, лимит 2 МБ, `noindex`.
  - Хoneypot формы спрятан корректно (`.hp-field` off-screen, `tabindex="-1"`, `autocomplete="off"`).
- **.htaccess:** HTTPS-редирект, HSTS, чистые URL, редирект `/admin/admin.php → /admin/` — корректно.
- **news.json** валиден: 10 записей, все картинки и ссылки на новости существуют.
- **Формы:** валидация (имя ≥2, телефон ≥10 цифр), honeypot, статусы загрузки — реализовано аккуратно.

---

## План правок (по приоритету)

1. ✅ CSS: `../../img/slide*` → `../img/slide*` (8 url).
2. ✅ objects.html: `000-00-00` → `388-63-71` (2 вхождения).
3. ✅ Унифицировать `?v=` на всех 37 страницах + `git add -A && git commit`.
4. ✅ `robots.txt` + `sitemap.xml` (37 URL, домен `sk-psp.ru`).
5. ⏳ JSON-LD `LocalBusiness` + OG-теги; один H1 на главной; дедупликация title двух О'Юность-новостей (добавить дату).
6. ⏳ width/height на img (минимум hero-картинки и объекты); ИНН/ОГРН в контакты; пункт про FormSubmit в privacy.
